---
permalink: functions/pages.json
permalinkBypassOutputDir: true
eleventyExcludeFromCollections: true
---
[{% for pages in collections.all %}
	{
   "title":"{% if pages.data.title %}{{ pages.data.title | jsonTitle}}{% else %}{{ meta.siteName }}{% endif %}",
   "slug":"{% if pages.data.title %}{{ pages.data.title | slug}}{% else %}home{% endif %}"
	}{% if forloop.last == false %},{% endif %}
{% endfor %}]